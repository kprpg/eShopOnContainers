apiVersion: v1
kind: Service
metadata:
    name: "myaks-323c"
    labels:
        app: "myaks-323c"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "myaks-323c"